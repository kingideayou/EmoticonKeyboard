<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/rl_emoticon"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_alignParentBottom="true"
    android:background="#dedede"
    android:orientation="vertical"
    android:visibility="gone"
    tools:layout_height="200dp"
    tools:visibility="visible">

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_emoticon"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/indicator"/>

    <me.relex.circleindicator.CircleIndicator
        android:id="@+id/indicator"
        android:layout_width="match_parent"
        android:layout_height="22dp"
        android:layout_above="@+id/tabs"/>

    <me.next.emoticonkeyboard.view.pagerslidingtabstrip.PagerSlidingTabStrip
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:background="#eeeeee"
        android:gravity="center"
        app:pstsDividerPadding="0dp"
        app:pstsIndicatorColor="#bbbbbb"
        app:pstsIndicatorHeight="40dp"
        app:pstsTabPaddingLeftRight="3dp"/>

</RelativeLayout>